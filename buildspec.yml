version: 0.2
phases:
  install:
    commands:
      - npm i npm@latest -g
      - npm install
  build:
    commands:
      - npm run build
      - npm run test
  post_build:
    commands:
      - aws s3 cp s3://bpdev-env/ormconfig.json ormconfig.json