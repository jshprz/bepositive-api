{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "kill-port": "^1.6.1",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "node kill_ports.js && lerna run start",
    "test": "lerna run test",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  }
}
